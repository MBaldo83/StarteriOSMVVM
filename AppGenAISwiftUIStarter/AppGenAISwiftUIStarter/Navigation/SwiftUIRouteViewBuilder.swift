import SwiftUI

@MainActor
class SwiftUIRouteViewBuilder {
    
    @ViewBuilder
    func view(for route: Router.Route) -> some View {
        Text("New View")
//        switch route {
//
//        }
    }
}
